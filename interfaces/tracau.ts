/** Tra cau response data. */
export interface TraCau {

  /** ID. */
  readonly _id: number;

  /** Fields. */
  readonly fields: {

    /** English field. */
    readonly en: string;

    /** Vietnamese field. */
    readonly vi: string;
  };
}
